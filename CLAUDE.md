@AGENTS.md

# Note for Claude Code

See [AGENTS.md](AGENTS.md) (imported above) for this repo's structure,
commands, and conventions. This file exists only because Claude Code
specifically looks for `CLAUDE.md`; the content itself is tool-agnostic and
lives in `AGENTS.md`.

One Claude-specific detail: in the commit message trailer format described in
AGENTS.md, use `Assisted-by: Claude <model name> <noreply@anthropic.com>` —
the actual model name you're running as, not a guess.
